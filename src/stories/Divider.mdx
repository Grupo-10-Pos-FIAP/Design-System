import { Canvas, Meta, Primary, Controls } from '@storybook/addon-docs/blocks';
import * as DividerStories from './Divider.stories';

<Meta of={DividerStories} />

# Divider

O componente `Divider` é usado para separar visualmente conteúdos ou agrupar elementos.

<Primary />

<Controls />

## Orientações

### Horizontal (Padrão)
<Canvas of={DividerStories.Default} />

### Vertical
<Canvas of={DividerStories.Vertical} />
