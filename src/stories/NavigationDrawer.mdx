import { Meta, Story, Canvas, Primary, Controls } from '@storybook/addon-docs/blocks';
import { NavigationDrawer } from '../components/NavigationDrawer/NavigationDrawer';
import * as NavigationDrawerStories from './NavigationDrawer.stories';

<Meta of={NavigationDrawerStories} />

# NavigationDrawer

O componente NavigationDrawer é um painel lateral deslizante que fornece navegação principal e acesso a funcionalidades da aplicação.

<Primary />

<Controls />

## Exemplos

### NavigationDrawer Padrão

Exemplo com controle interno de estado e estrutura completa:

<Canvas of={NavigationDrawerStories.Default} />
