import { Meta, Story, Canvas, Primary, Controls } from '@storybook/addon-docs/blocks';
import { IconButton } from '../components/IconButton/IconButton';
import * as IconButtonStories from './IconButton.stories';

<Meta of={IconButtonStories} />

# IconButton

O componente IconButton é um botão compacto que utiliza um ícone como conteúdo principal, ideal para ações secundárias, controles de interface e espaços limitados.

<Primary />

<Controls />

## Exemplos

### Estado Padrão

<Canvas of={IconButtonStories.Default} />
