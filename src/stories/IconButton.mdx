import { Meta, Story, Canvas, Primary, Controls } from '@storybook/addon-docs/blocks';
import { IconButton } from '../components/IconButton/IconButton';
import * as IconButtonStories from './IconButton.stories';

<Meta of={IconButtonStories} />

# IconButton

O componente IconButton é um botão compacto que utiliza um ícone como conteúdo principal, ideal para ações secundárias, controles de interface e espaços limitados.

<Primary />

<Controls />

## Exemplos

### Variantes

**Primary** - Variante primária para ações principais
<Canvas of={IconButtonStories.Primary} />

**Secondary** - Variante secundária para ações complementares
<Canvas of={IconButtonStories.Secondary} />

**Outlined** - Variante com borda para ações menos proeminentes
<Canvas of={IconButtonStories.Outlined} />

**Transparent** - Variante transparente para interfaces minimalistas
<Canvas of={IconButtonStories.Transparent} />


### Tamanhos

**Small** - Tamanho pequeno para interfaces compactas
<Canvas of={IconButtonStories.Small} />

**Medium** - Tamanho médio (padrão)
<Canvas of={IconButtonStories.Medium} />

**Large** - Tamanho grande para maior destaque
<Canvas of={IconButtonStories.Large} />


### Estados

**Disabled** - Estado desabilitado para ações indisponíveis
<Canvas of={IconButtonStories.Disabled} />

