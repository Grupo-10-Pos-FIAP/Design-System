import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { Header } from '../components/Header/Header';
import * as HeaderStories from './Header.stories';

<Meta of={HeaderStories} />

# Header

O componente Header é a barra de navegação superior da aplicação, responsável por fornecer acesso rápido a funcionalidades principais e informações do usuário.

## Exemplos

### Header Básico

Exemplo do componente Header com avatar e logo SVG como componente.

<Canvas of={HeaderStories.Default} />

### Header com URL do SVG

Exemplo usando a URL direta do SVG. Útil quando você quer carregar a logo diretamente do diretório público.

<Canvas of={HeaderStories.WithSvgUrl} />

### Header com Ações de Clique

Exemplo com handlers de clique na logo e no avatar. Perfeito para navegação ou abertura de menus.

<Canvas of={HeaderStories.WithClickActions} />

### Header Sem Avatar

Versão minimalista do Header sem o componente de avatar. Ideal para páginas públicas ou quando o usuário não está logado.

<Canvas of={HeaderStories.WithoutAvatar} />